import type { JSX } from "react";

export default function DashboardPage(): JSX.Element {
  return (
    <main>
      <h1>Pulse</h1>
      <p>
        The dashboard (application status cards, uptime) is built in sprint 7. This page exists so
        the web app has a working route in sprint 1.
      </p>
    </main>
  );
}
