# Copy to apps/web/.env.local (Next.js loads .env.local automatically; it's gitignored).

# Must include the /api/v1 prefix main.ts sets as the global route prefix.
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1
